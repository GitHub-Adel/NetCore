(SocialMedia.Api/Response)

1) create a generic class

    //convierte a Data en el object que reciba por el constructor
    //debe crear un object y pasarlo ApiResponse<Object>    
    public class ApiResponse<T>
    {
        public ApiResponse(T data)
        {
            this.Data=data;
        }

        public T Data { get; set; }
    }

2) modo de uso:  se usa en el controller.

  //lo que se hace es especificar el tipo e inyectarle un objeto de ese tipo.
  var response = ApiResponse<TipoObjeto>(objecto);
  //ahora podemos hacer
  response.Data.propiedades que tenga el object inyectado.

 