Esta interfaces hereda los metodos comunes de IBaseRepository y define los propios.
toda la logica de acceso a dato(todo lo que toca la DB) se pone aqui: update,delete etc.
Se construye directamente en el constructor de UnitOfWork a traves de propiedades. 
no inyectar por el startup.cs
======================================================
interface: define los method, solo la firma. (abstraction)

1) create file
2) IUserRepository.cs
3) elegir interface



namespace SocialMedia.Core.Interface
{
    public interface IUserRepository:IBaseRepository<User>
    {
        User GetByPhone(string phone);
    }
}



hereda IBaseRepository e implementa los metodos de la IUserRepository.
======================================================

1) create file
2) UserRepository.cs
3) elegir class


namespace SocialMedia.Infrastructure.Repository
{
    public class UserRepository : BaseRepository<User>, IUserRepository
    {
        public UserRepository(SocialmediaDBContext _context) : base(_context)
        {
        }
        //este metodo es propio de esta class
        //_entity se hereda de BaseRepository
        public User GetByPhone(string phone)
        {
            return _entity.FirstOrDefault(x=>x.Phone.Equals(phone));
        }
    }
}

